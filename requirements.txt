# ESL Real-Time Sign Language Recognition
# Install: pip install -r requirements.txt
#
# Supported: Python 3.10–3.13 (see pyproject.toml requires-python).
# Windows: run .\setup_venv.ps1 once after installing Python 3.11+.
# GPU is NOT required - all inference runs on CPU.

# -- Core ML -----------------------------------------------------------
tensorflow>=2.13,<=2.18          # pin 2.18.0 for project venv; TF 2.16+ needs tf-keras for DeepFace
tf-keras                          # required by retina-face / DeepFace when using TF 2.16+
numpy>=1.26
scipy>=1.11

# -- Computer Vision ---------------------------------------------------
opencv-python>=4.8
mediapipe>=0.10.30,<0.11   # HolisticLandmarker tasks API; 0.10.9–0.10.21 crash on partial hand detect

# -- Emotion Detection -------------------------------------------------
deepface>=0.0.89,<0.1       # wraps VGG-Face, FaceNet etc.; install triggers model downloads

# -- Evaluation & Visualisation ----------------------------------------
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12

# -- Phase 4 - Web App ------------------------------------------------
fastapi>=0.103,<1.0
uvicorn[standard]>=0.24
websockets>=12.0            # required by uvicorn for WebSocket support
python-multipart>=0.0.6     # required by FastAPI for form data

# -- Data download -----------------------------------------------------
kagglehub>=0.2
