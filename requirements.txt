# ESL Real-Time Sign Language Recognition
# Install: pip install -r requirements.txt
#
# Supported: Python 3.10 or 3.11 (see pyproject.toml requires-python).
# Python 3.13 is not supported by the TensorFlow version range below on Windows.
# Windows: run .\setup_venv.ps1 once after installing Python 3.11.
# GPU is NOT required - all inference runs on CPU.

# -- Core ML -----------------------------------------------------------
tensorflow>=2.16,<2.18      # Keras 3 bundled; models saved as .keras format
numpy>=1.24,<2.0
scipy>=1.11

# -- Computer Vision ---------------------------------------------------
opencv-python>=4.8,<4.12
mediapipe==0.10.20          # PINNED - mp.solutions.holistic removed in later versions

# -- Emotion Detection -------------------------------------------------
deepface>=0.0.89,<0.1       # wraps VGG-Face, FaceNet etc.; install triggers model downloads

# -- Evaluation & Visualisation ----------------------------------------
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12

# -- Phase 4 - Web App ------------------------------------------------
fastapi>=0.103,<1.0
uvicorn[standard]>=0.24
python-multipart>=0.0.6     # required by FastAPI for form data

# -- Data download -----------------------------------------------------
kagglehub>=0.2
